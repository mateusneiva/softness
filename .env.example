# Public API URL used by the Next.js client (browser).
# Must be set at Docker/image build time (NEXT_PUBLIC_* is baked into the bundle).
NEXT_PUBLIC_API_URL=http://localhost:5555

# Port used by `next start` / Docker (host + container).
PORT=3000
